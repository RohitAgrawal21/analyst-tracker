# Broker PDFs are copyright + bulky: they NEVER go to GitHub. Only extracted data does.
reports/
*.pdf

# All local runtime state: DB, price cache, telegram session/state, logs, temp
data/
staging/
telegram_review/
__pycache__/
*.pyc
.venv/

# Secrets
.env
telegram_config.json
*.session
*.session-journal
